# Lab 14 — Managing Storage

## Focus

Practise GPT partitions, filesystems, labels, UUIDs, swap, manual mounts, and persistent mounts through `/etc/fstab`.

## Expected evidence

Capture block device state, filesystem creation, mount behaviour, fstab entries, swap status, and reboot persistence.
